sudo nano /etc/postfix/sasl_passwd
[smtp.hostinger.com]:587 mail@redes2.xyz:Rosario+vampire25

/etc/postfix/main.cf
# Identidad del servidor
myhostname = redes2.xyz
mydomain = redes2.xyz
myorigin = $mydomain

# Destinos locales
mydestination = $myhostname, localhost.$mydomain, localhost, $mydomain


# Rutas de alias
alias_maps = hash:/etc/aliases
alias_database = hash:/etc/aliases

# Configuración de red
mynetworks = 127.0.0.0/8 [::1]/128
inet_interfaces = all
inet_protocols = all

# Buzones de usuario
home_mailbox = Maildir/
mailbox_size_limit = 0
recipient_delimiter = +

# Relay SMTP de Hostinger
relayhost = [smtp.hostinger.com]:587
smtp_use_tls = yes
smtp_tls_security_level = encrypt
smtp_tls_CAfile = /etc/ssl/certs/ca-certificates.crt

# Autenticación SASL
smtp_sasl_auth_enable = yes
smtp_sasl_password_maps = hash:/etc/postfix/sasl_passwd
smtp_sasl_security_options = noanonymous
smtp_sasl_mechanism_filter = plain,login


sudo nano /etc/dovecot/dovecot.conf

protocols = imap pop3 lmtp

sudo nano /etc/dovecot/conf.d/10-mail.conf
mail_location = maildir:~/Maildir

sudo nano /etc/dovecot/conf.d/10-auth.conf
disable_plaintext_auth = no
auth_mechanisms = plain login



telnet 184.73.136.1 143
# O, si tienes el dominio propagado:
telnet redes2.xyz 143


swaks --to tu_destino@gmail.com \
--from mail@redes2.xyz \
--server smtp.hostinger.com:587 \
--auth LOGIN --auth-user mail@redes2.xyz --auth-password 'TU_CONTRASEÑA_SMTP' \
--tls

